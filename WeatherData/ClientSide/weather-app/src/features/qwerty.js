import { createSlice } from '@reduxjs/toolkit'

const initialState = {

}

const qwerty = createSlice({
    name: sliceName,
    initialState,
    reducers: {
    
    }
});

export const {

} = qwerty.actions
export default qwerty.reducer